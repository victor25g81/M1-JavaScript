// Ex03: Crear una función que imprima la edad y el nombre de una persona.
// Se deben crear parámetros para recibir eos dos valores.
// Las variables deben ser creadas con let o var.

function edad_nombre(nombre, edad){
    console.log('Su nombre es:', nombre);
    console.log('Su edad es:', edad);
}

let nombre = 'victor';
let edad = '40';

edad_nombre(nombre, edad);

// NOTA: Úsese let mientras que NO sea necesario utilizar var.
